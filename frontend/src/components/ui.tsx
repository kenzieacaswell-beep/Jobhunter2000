import {ReactNode,useEffect} from 'react';
import {X,AlertCircle,CheckCircle2,Loader2} from 'lucide-react';

export function Button({children,variant='primary',loading,...props}:React.ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'secondary'|'ghost'|'danger';loading?:boolean}){return <button {...props} className={`btn ${variant} ${props.className||''}`} disabled={loading||props.disabled}>{loading?<Loader2 size={16} className="spin"/>:null}{children}</button>}
export function Empty({title,body,action}:{title:string;body:string;action?:ReactNode}){return <div className="empty-state"><div className="empty-orbit">✦</div><h3>{title}</h3><p>{body}</p>{action}</div>}
export function Drawer({open,onClose,children,title}:{open:boolean;onClose:()=>void;children:ReactNode;title:string}){useEffect(()=>{const close=(event:KeyboardEvent)=>event.key==='Escape'&&onClose();window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[onClose]);if(!open)return null;return <div className="drawer-wrap" role="presentation"><div className="drawer-scrim" onClick={onClose}/><aside className="drawer" role="dialog" aria-modal="true" aria-label={title}><header><span className="eyebrow">JOB DETAIL</span><button className="icon-btn" onClick={onClose} aria-label="Close detail"><X/></button></header>{children}</aside></div>}
export function Toast({message,onUndo,onClose}:{message:string;onUndo?:()=>void;onClose:()=>void}){useEffect(()=>{const id=window.setTimeout(onClose,5000);return()=>window.clearTimeout(id)},[onClose]);return <div className="toast"><CheckCircle2 size={18}/><span>{message}</span>{onUndo&&<button onClick={onUndo}>Undo</button>}<button aria-label="Dismiss notification" onClick={onClose}><X size={15}/></button></div>}
export function ErrorState({message,retry}:{message:string;retry:()=>void}){return <div className="error-state"><AlertCircle/><div><strong>Couldn’t load this view</strong><p>{message}</p></div><Button variant="secondary" onClick={retry}>Try again</Button></div>}
